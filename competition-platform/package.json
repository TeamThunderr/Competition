{
    "name": "competition-platform",
    "version": "1.0.0",
    "description": "Root package to run both frontend and backend",
    "scripts": {
        "start": "npm run start --prefix backend",
        "client": "npm run dev --prefix frontend",
        "dev": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\""
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "concurrently": "^9.1.0",
        "multer": "^2.0.2"
    }
}
